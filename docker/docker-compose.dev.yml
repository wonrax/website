version: '3.1'

services:
  database:
    extends:
      file: docker-compose.yml
      service: database
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - 8081:8080
